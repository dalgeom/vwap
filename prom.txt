
============================================
  VWAP Trader - 세션 인계 프롬프트
  (2026-05-13 기준)
============================================

git pull 후 새 세션 첫 메시지로 붙여넣으세요.

--------------------------------------------------------------

# VWAP Trader 세션 인계

## 프로젝트 위치
`c:\Users\PC\Desktop\현진\code` (다른 PC면 경로 확인)

## 현재 상태 (2026-05-13)
- **봇**: 실행 중 (momentum_bot.py) — 데모 계좌, 데이터 수집 목적
- **오픈 포지션**: 수시로 변동 (state_momentum.json 확인)
- **전략**: 모멘텀 봇 (5분봉, 40코인 유니버스)

--------------------------------------------------------------

## 전략 전체 흐름 (6개월 요약)

### 1단계: EMA 전략 (폐기됨)
- EMA9/21 크로스 + ADX>20, ATR 기반 SL/TP, 15분봉
- 백테스트 결과: BTC/ETH 승률 28% → EV 음수 → 폐기

### 2단계: 펀딩 역추세 (Funding Contrarian) — 폐기됨
- |펀딩비| > 임계값 구간 역방향 진입, 8h 후 청산
- 원래 백테스트 결과(EV +0.506R, Sharpe 3.31)가 SL 시뮬 오류로 모두 무효화됨
- SL 시뮬 오류: 8h 종가 1회 체크(잘못) → 실제는 매봉 low/high 체크 → SL 비율 41%→83%
- 정확한 시뮬 결과: 모든 SL 조합에서 EV 음수 → 폐기

### 3단계: 모멘텀 봇 — 현재 전략 (데이터 수집 중)
- 5분봉, 40코인 유니버스 스캔
- ATR 기반 SL/TP, 리스크 per trade ~0.5% 잔고
- 데모 계좌에서 실시간 운영 중 (가짜 돈, 데이터 수집이 목적)
- 일일 손실 한도 비활성화됨 (데이터 수집 목적으로 의도적 비활성화)

--------------------------------------------------------------

## 2026-05-12 실전 운영 성과 (35건, 약 9시간)

| 항목 | 값 |
|------|----|
| 총 거래 | 35건 |
| 승률 | 14.3% (5승 30패) |
| 평균 EV | -1.41% / 거래 |
| 추정 달러 P&L | -$6,183 |
| 평균 보유 시간 | 84분 |

### 방향별
- Long 17건: 승률 11.8%, EV -0.59%
- Short 18건: 승률 16.7%, EV -2.19%
- → 2026-05-12는 전반적 상승장 → 숏 전략 특히 불리했음

### 주요 승리 거래
- SAGAUSDT long +14.21% ($+502) — 전체 수익의 29%가 이 1건
- BCHUSDT short 2건 합산 +$907
- CRVUSDT short +$244

### 주요 손실 패턴
- GIGAUSDT: 같은 날 long -$258 + short -$241 = -$499 (극단 변동성)
- BTCUSDT: 2건 -$714 (포지션 크고 SL 촘촘)
- SAGAUSDT: 3건 net -$51 (1 big win + 2 big losses)

### 진단
- 35건은 통계 노이즈 범위 → 판단 보류
- 최소 200~300건 쌓인 후 전략 EV 유효 판단
- 현 속도 하루 ~35건, 약 1주일이면 250건 예상

--------------------------------------------------------------

## 이번 세션에서 수정한 것

### 버그 수정: qty 부동소수점 오류
- **파일**: `vwap_trader/src/vwap_trader/core/position_sizer.py`
- **증상**: ETHUSDT `qty=21.240000000000002`, SAGAUSDT `qty=86517.90000000001` → Bybit `Qty invalid (ErrCode: 10001)` 주문 실패
- **원인**: `floor(clamped_qty / lot_size) * lot_size` — float 곱셈 부동소수점 오류
- **수정**: Python `Decimal` 모듈로 정확한 계산
```python
# 수정 전 (잘못됨)
from math import floor
qty = floor(clamped_qty / lot_size) * lot_size

# 수정 후
from decimal import Decimal, ROUND_DOWN
lot_d = Decimal(str(lot_size))
qty = float(
    (Decimal(str(clamped_qty)) / lot_d).to_integral_value(ROUND_DOWN) * lot_d
)
```

--------------------------------------------------------------

## 핵심 파일

```
vwap_trader/
├── src/vwap_trader/
│   ├── momentum_bot.py              # 모멘텀 봇 메인 (현재 실행 중)
│   ├── strategy/momentum.py        # 모멘텀 신호 로직
│   ├── core/position_sizer.py      # qty 계산 (Decimal 수정됨)
│   ├── infra/bybit_client.py       # 데모 계좌 + 공개 API 펀딩비
│   └── scripts/
│       └── regime_analysis.py      # 펀딩 역추세 SL sweep 분석 (폐기 근거)
├── data/
│   ├── trades_momentum.jsonl       # 청산 거래 기록 (35건+ 누적 중)
│   ├── state_momentum.json         # 봇 포지션 상태
│   └── slippage_momentum.jsonl     # 슬리피지 기록
└── config/.env                     # API 키 (데모)
```

## 봇 실행 방법
```bash
cd vwap_trader/src
python -m vwap_trader.momentum_bot
```

--------------------------------------------------------------

## 세션 시작 후 확인할 것

1. `vwap_trader/data/state_momentum.json` — 현재 포지션 확인
2. `vwap_trader/logs/bot.log` 마지막 20줄 — 봇 정상 여부
3. `vwap_trader/data/trades_momentum.jsonl` 줄 수 — 거래 누적 현황
4. 250건 이상이면 전략 EV 분석 시작

--------------------------------------------------------------

## 알려진 이슈

- **DOGEUSDT**: Bybit 화면에 거래 기록 있으나 trades_momentum.jsonl에 누락됨 → 로깅 버그 가능성 (미수정)
- **일일 손실 한도**: 데이터 수집 목적으로 의도적 비활성화 상태 유지

--------------------------------------------------------------

## 전략 원칙 (유지)
- Simple Is Best
- 백테스트 SL 시뮬은 반드시 "매봉 low/high 체크" 방식으로
- 데모 계좌 API 펀딩비는 고정값 반환 → 반드시 공개 API 사용
- 슬리피지 0.10% 이상 항상 반영

--------------------------------------------------------------
